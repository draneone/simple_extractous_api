# Logging level
RUST_LOG=info

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# OCR languages (use + to combine multiple)
# Common codes: eng, rus, fra, deu, spa, ita, por, chi-sim, jpn, kor, ara
TESSERACT_LANGUAGES=eng+rus

# CORS allowed origins (comma-separated, default: '*')
# Use '*' for all origins or specify specific domains
ACTIX_CORS_ORIGIN=*
# ACTIX_CORS_ORIGIN=https://myapp.com,https://admin.myapp.com

# Bearer token for authentication (optional)
# If set, /parse/files and /parse/urls require 'Authorization: Bearer <token>' header
# ACTIX_BEARER_TOKEN=your_secret_token_here
